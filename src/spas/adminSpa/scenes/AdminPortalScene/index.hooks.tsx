import { useDispatch, useSelector } from "react-redux";
import { selectors } from "../../redux-store/slices";
import { actions } from "@/spas/adminSpa/redux-store/slices";

export const useAdminPortalScene = () => {
  return {};
};
